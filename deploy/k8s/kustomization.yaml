apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      app.kubernetes.io/part-of: fakery
    includeSelectors: false

resources:
  - namespace.yaml
  - secret.yaml
  - rbac.yaml
  - postgres.yaml
  - redis.yaml
  - minio.yaml
  - migrate-job.yaml
  - api.yaml
  - worker.yaml
